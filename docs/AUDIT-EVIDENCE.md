# Matriz de evidências da auditoria

Este documento separa itens implementáveis no workspace de validações que exigem ambiente externo. Um item só deve ser marcado como concluído no `TODO-AUDITORIA.md` quando a evidência indicada existir.

| Item | Estado atual | Evidência para concluir |
| --- | --- | --- |
| Guardas de transição em PlayerStats e troca de faixa | Corrigido e typecheckado | `playerstats` e `playbackmanager` agora retornam antes de consultar `ServerId` quando não há item/servidor ativo durante parada ou troca de reprodução; `npm run build:check` passou. O lint focado do módulo ainda expõe 61 débitos legados de tipagem/complexidade. |
| Falhas silenciosas no carregamento de reprodução | Corrigido e validado | O carregamento assíncrono de plugins de mídia agora registra rejeições, e o diálogo “próximo vídeo” limpa seu estado e libera o slot global quando a consulta de preferências falha; isso evita rejeição não observada e diálogo preso impedindo novas tentativas. `npm run build:check`, 194 testes e `npm run lint:changed` passaram; o gate ficou em 0 erros e 42 warnings. |
| Runner Playwright com credencial previsível e comandos dependentes de PATH | Corrigido e validado | O fallback de senha do usuário administrativo E2E agora é aleatório por execução (`crypto.randomBytes`), e `taskkill.exe`/`dotnet.exe` são resolvidos por caminhos Windows confiáveis antes da execução do stage. O runner também separa preparação do stage, montagem dos argumentos e o relatório de ausência de specs. Os suportes e specs Playwright foram saneados contra senhas previsíveis, regexes superlineares, callbacks vazios, ternários aninhados e complexidade excessiva; `node --check`, ESLint de `tests/playwright`, testes de credenciais (2/2), `npm run build:check` e `npm run lint:changed` passaram sem erros. |
| Descoberta de servidor atrás de reverse proxy | Corrigido e testado | `bootstrap`, `dashboard`, `ConnectionRequired` e `connectionManager` agora compõem endpoints com `getServerEndpoint`, preservando prefixos como `/mulletaflix` e eliminando `//System/Info/Public` quando a URL configurada termina em `/`. `src/utils/url.test.ts` passou 11/11, `npm run build:check` e ESLint direcionado passaram. |
| Corrida de inicialização do MariaDB embutido | Corrigido e compilado | O log anexado mostrou duas instâncias disputando o bootstrap: a segunda recebeu TCP antes de o primeiro MariaDB estar pronto, abriu outro `mysqld` sobre o mesmo `ibdata1` e terminou com `The data file './ibdata1' must be writable`/timeout. `MariaDbProcessManager` agora usa mutex nomeado, não inicia outro processo quando a porta 3306 já está ocupada, aguarda conexão SQL real, valida escrita do diretório, verifica o exit code do inicializador e limpa o processo filho/mutex quando o startup falha. `dotnet build Jellyfin.Server/Jellyfin.Server.csproj --no-restore` passou. |
| MariaDB em loopback IPv4 e probe SQL instalado | Corrigido e testado | O log de 2026-09-12 mostrou o servidor pronto em `127.0.0.1`, enquanto o bootstrap e a configuração persistida usavam `localhost`, causando timeout SQL apesar de `mysqld` estar pronto. O manager, fallback de configuração, provider, factory de design-time e fábrica de integração agora usam/normalizam `127.0.0.1`; o bootstrap local também explicita `SslMode=None`, e falhas do probe SQL são registradas com a exceção completa. O caminho de reuso de um MariaDB externo não acessa mais um PID nulo. A busca por `Server=localhost;Port=3306` não encontrou usos operacionais fora de fixtures de teste. `EncodedQueryStringTest` passou 8/8, `Jellyfin.Server.Tests` 16/16 e a integração passou 111/114, com 3 ignorados e 0 falhas. |
| Upload de letras sem `Content-Length` | Corrigido e compilado | `LyricsController.UploadLyrics` não rejeita mais prematuramente requisições chunked por tratar `Content-Length` ausente como corpo vazio; o tamanho efetivo é validado após a cópia, mantendo o limite de 1 MB e a rejeição de corpo vazio. `dotnet build Jellyfin.Api/Jellyfin.Api.csproj --no-restore` passou com 0 erros. |
| Stage desatualizado e cópia incompleta no builder | Corrigido e validado | O servidor foi republicado em Release; `Copy-DirectoryContents` deixou de embutir aspas nos argumentos do `robocopy`, e `icon.ico`/`LICENSE` passaram a ser preservados durante republicações. O stage contém servidor, runtime, web, tray, MariaDB e extras; `validate-stage.ps1` passou. O instalador final foi gerado com 396.139.474 bytes e SHA-256 `4E58B7C3301C3181C7AC946D24EB9126127912C72C4B749A8364E48F973ABB89`. |
| Instrução de execução do stage inválida | Corrigido e validado | O builder não referencia mais o inexistente `stage-start.ps1`; a saída agora informa `stage\MulletaFlix.exe --datadir stage-data`. O parser PowerShell e a validação de integridade do stage passaram. |
| Exaustão de conexões em testes paralelos | Corrigido e testado | `Minimum Pool Size=10` multiplicava dez conexões por banco temporário e atingia `Too many connections` no MariaDB durante a suíte paralela. O pool agora inicia sob demanda (`Minimum Pool Size=0`); a suíte completa de integração passou 111/114, com 3 testes ignorados e 0 falhas. |
| Guarda do OSD durante parada | Corrigido e typecheckado | O clique de alternância de tempo restante agora ignora o evento quando `PlayState` ou `NowPlayingItem` já foi removido; `npm run lint:changed` passou sem erros e `npm run build:check` passou. |
| Tipagem do Web Component `emby-progressring` | Corrigido e validado | O protótipo legado do anel de progresso deixou de usar casts `any`; callbacks, observador de mutações e método `setProgress` agora têm interfaces explícitas, preservando o registro e a renderização dos quatro segmentos. `npm run build:check`, ESLint focalizado, `npm run lint:changed` e a suíte frontend (196/196) passaram. |
| Estado stale dos controles de playlist | Corrigido e testado | O OSD agora oculta e desabilita os botões anterior/próximo quando a playlist tem zero ou um item, evitando controles residuais após troca de mídia; TypeScript e 192 testes frontend passaram. |
| Smoke test com elevação UAC e caminhos relativos | Corrigido e validado sintaticamente | `Test-CleanInstaller.ps1` agora encaminha ao processo elevado os caminhos absolutos já resolvidos do instalador, diretório de instalação e diretório de dados; parser PowerShell passou sem erros. |
| Medição global atual do ESLint | Diagnóstico atualizado | Execução completa de `npm run lint -- --format json` encontrou 1.522 erros e 104 warnings em 934 arquivos; o gate incremental dos arquivos alterados permanece em 0 erros, agora verificando 229 arquivos. A árvore `tests/playwright` foi revalidada isoladamente com 0 erros e 0 warnings. A árvore Selenium/Cucumber caiu para 72 achados legados, todos concentrados em `require`/nomes de steps DSL. |
| `FfToolKeyframeExtractor` | Não é defeito local | O método público que lança `NotImplementedException` não possui consumidores nem testes no workspace e permanece idêntico à implementação oficial upstream; o caminho efetivamente registrado usa `FfProbeKeyframeExtractor`. |
| Diagnóstico de falha no bind do Kestrel | Corrigido e testado | `Jellyfin.Server/Program.cs` agora inclui a exceção original no `LogError` antes de relançá-la, preservando diagnóstico de porta/endereço; `Jellyfin.Server.Tests` passou 16/16. |
| Parser de keyframes com saída inválida | Corrigido e testado | `FfProbeKeyframeExtractor.ParseStream` agora ignora registros sem separadores esperados em vez de lançar durante a extração; o teste focado passou 3/3. |
| Exceção não observada no timer de transcodificação | Corrigido e testado | `OnTranscodeKillTimerStopped` agora valida o estado recebido e captura/loga falhas de reinício ou encerramento do job; `Jellyfin.MediaEncoding.Tests` passou 60/60. |
| Exceção não observada ao disparar tarefa agendada | Corrigido e testado | `ScheduledTaskWorker.OnTriggerTriggered` agora valida o sender, isola falhas ao enfileirar e reiniciar o trigger e registra o contexto; `Jellyfin.Server.Implementations.Tests` passou 764/764, com 38 ignorados. |
| Falha de descarte durante shutdown de sessões | Corrigido e testado | `SessionManager.OnApplicationStopping` agora captura falhas ao descartar cada WebSocket e continua fechando as demais sessões; `Jellyfin.Server.Implementations.Tests` passou 764/764, com 38 ignorados. |
| Instalação do instalador em ambiente limpo | Pendente externo | O host atual não possui Windows Sandbox/Hyper-V disponível e não está elevado, portanto não permite concluir a elevação UAC de forma observável. `Test-CleanInstaller.ps1` verifica instalação silenciosa, serviço, `/health`, tray, ACL do serviço e desinstalação; agora aceita `-AutoElevate` para relançar o próprio smoke test via UAC preservando os parâmetros. |
| Suíte de integração com MariaDB | Corrigido e testado | Após corrigir o endereço de loopback e a pré-alocação do pool, `dotnet test tests/Jellyfin.Server.Integration.Tests/Jellyfin.Server.Integration.Tests.csproj --no-build --no-restore --verbosity minimal` passou 111 testes, ignorou 3 e não registrou falhas. A fábrica usa banco temporário por execução e o remove ao finalizar. |
| Segurança do smoke test | Corrigido | `Test-CleanInstaller.ps1` agora recusa sobrescrever diretórios ou serviço existentes por padrão; `-AllowExistingPaths` é necessário para uma execução deliberadamente destrutiva/upgrade. |
| Bloqueio síncrono no DtoService | Mitigado | `GetBaseItemDto` evita `.GetAwaiter().GetResult()` para itens comuns sem Trickplay/Live TV; o bloqueio permanece somente nos enriquecimentos que ainda são assíncronos e documentados para migração futura. |
| `/health` retornando 500 | Corrigido no código | A causa era `AddDbContextCheck` tentando resolver `MulletaFlixDbContext` diretamente, enquanto a aplicação registra `IDbContextFactory`. O novo `MulletaFlixDatabaseHealthCheck` cria o contexto pela factory e testa `CanConnectAsync`; a validação runtime local confirmou o erro original antes da correção. |
| Regressão do health check do banco | Testado | `MulletaFlixDatabaseHealthCheckTests` cobre banco acessível e falha da factory; execução focada passou 2/2 testes. |
| Instalador após correção do `/health` | Recompilado e validado | Backend republicado, frontend reconstruído e NSIS recompilado. Tamanho: 396.135.955 bytes; SHA-256 `AEA34BC3836B06A642CF7DF97F5D25D7E3492849EE6E6A81DB0462FF3E6150E1`; `validate-stage.ps1` passou. |
| Instalador após correção das preferências de exibição | Recompilado e validado | Frontend reconstruído com o estado de erro/retry e correção do `userId` opcional; NSIS recompilado. Tamanho: 396.136.197 bytes; SHA-256 `24BBC2245D1CB9D2EAA6F1D2F44850C0669BD93B242453F7C8C35D0B2A554E89`; `validate-stage.ps1` passou. |
| Instalador após correção do Networking | Recompilado e validado | A falha de configuração não fica mais escondida por loading infinito; frontend e NSIS foram recompilados. Tamanho: 396.135.765 bytes; SHA-256 `04DA8B165BF6CFFBD4B07CE965530BECB52F664780488994F849D5C1AFC9A715`; `validate-stage.ps1` passou. |
| Upload de letras com limite real | Corrigido e compilado | O endpoint agora usa `[RequestSizeLimit(1 MB)]`, protegendo também requisições chunked sem `Content-Length`; `Jellyfin.Api` compilou com 0 erros e 0 avisos. |
| Instalador final com limite de upload | Recompilado e validado | Backend e frontend republicados, NSIS recompilado. Tamanho: 396.135.774 bytes; SHA-256 `01A9C84D2221AC7FC88A52E9A61869112E21B0024E40FE2CE5A2E5EF2BAE6C64`; `validate-stage.ps1` passou. |
| Estados de erro antes ocultos por loading | Corrigido e testado | Display, Live TV, Transcoding, Plugins, Repositórios, Backups, Trickplay, gravações e Nebula agora renderizam o erro/retry antes do fallback de loading. Typecheck passou e a suíte frontend passou 21 arquivos/192 testes. |
| Instalador final com matriz de loading ampliada | Recompilado e validado | Frontend/backend republicados, NSIS recompilado. Tamanho: 396.136.325 bytes; SHA-256 `FE9ED15D701B2AA028A5D7A98295FC5A10BF5BCFD3FE1044084B742C89E9E97E`; `validate-stage.ps1` passou. |
| Matriz adicional de erros em dashboards | Corrigido e testado | Usuários, licenças, Branding, tarefas, Settings, Resume, Streaming, Libraries, Metadata, Midia Storage Online e NFO não retornam mais loading infinito quando a API falha; typecheck e 21 arquivos/192 testes frontend passaram. |
| Instalador final com matriz de erro completa da rodada | Recompilado e validado | Frontend/backend republicados, NSIS recompilado. Tamanho: 396.136.052 bytes; SHA-256 `7B75D9735F2EF7850F15C82AAD884A0EEB12439BAB706C6088663CD1C334B34E`; `validate-stage.ps1` passou. |
| Fila de trabalhos e perfil estável | Corrigido e testado | Ambos agora exibem erro e Retry quando a consulta inicial falha, sem ficar presos no spinner; typecheck e 21 arquivos/192 testes frontend passaram. |
| Instalador final da auditoria atual | Recompilado e validado | Frontend/backend republicados, NSIS recompilado. Tamanho: 396.136.660 bytes; SHA-256 `7333D105FC2B1DCD50CE1F6FD6AAA0252E8D42F2568F885572FD8FFAA6EA1CBA`; `validate-stage.ps1` passou. |
| Matriz residual de loading/erro | Corrigida e testada | Busca, sugestões, recomendações, próximos episódios, gêneros e TV ao vivo agora exibem erro anunciável e Retry quando as consultas falham, evitando spinner indefinido. TypeScript passou e a suíte frontend aprovou 21 arquivos/192 testes. |
| Instalador final após matriz residual | Recompilado e validado | Frontend/backend republicados, NSIS recompilado e stage validado. Tamanho: 396.137.200 bytes; SHA-256 `D981E3CE4618B24C38637964E4FABAD8F3987B4207E0B1FCD098A67D419D9290`. |
| Lint dos novos callbacks de Retry | Corrigido e validado | O lint direcionado passou sem erros após substituir callbacks inline por `useCallback` e eliminar sombras de variáveis nos hooks de preferências; typecheck e 192 testes continuam aprovados. |
| Instalador final após lint residual | Recompilado e validado | Frontend/backend republicados, NSIS recompilado e stage validado. Tamanho: 396.137.537 bytes; SHA-256 `80CE71ADB4581B6B4FEA490A151562C57CF56ED584B0EC3C1661AC62A66BE4C2`. |
| Smoke test administrativo | Bloqueado pelo ambiente | `Test-CleanInstaller.ps1` com `-AutoElevate`, diretórios isolados e porta 18096 retornou código 1 na elevação UAC antes da instalação; verificações posteriores confirmaram ausência de serviço e de resíduos temporários. |
| Conversor de enums Flags | Corrigido e testado | `JsonFlagEnumConverter<T>` agora desserializa arrays de flags, combina valores, valida nomes desconhecidos e lança `JsonException` para payload inválido. Teste focado: 5 aprovados. |
| Instalador final com correção do conversor JSON | Recompilado e validado | Backend/frontend republicados, NSIS recompilado e stage validado. Tamanho: 396.137.973 bytes; SHA-256 `BA85ED83675D6D8A9C14781B49AFEC7747FC2229910E985BC33B27FE62A8AF61`. |
| Varredura residual de loading | Comprovada | A busca por retornos de `Loading` restantes encontrou apenas estados já precedidos por erro/retry ou fluxos de conexão intencionalmente controlados; não restou spinner de consulta sem caminho de falha identificável. |
| Runtime da instalação existente | Diagnóstico confirmado | O binário atualmente instalado em `C:\Program Files\MulletaFlix\Server` iniciou com `--datadir C:\ProgramData\MulletaFlix\Server` e completou o startup em ~9 s, mas `/health` retornou 500 pelo registro antigo `DbContextHealthCheck<MulletaFlixDbContext>`. O instalador novo contém a substituição por `MulletaFlixDatabaseHealthCheck`; upgrade ainda é necessário para validar a correção em runtime. |
| Instalador após mitigação do DtoService | Recompilado e validado | Backend republicado, frontend reconstruído e NSIS recompilado. Tamanho: 396.135.458 bytes; SHA-256 `CE4C131D5E3D82E4522E9E2787AE884B5A10CA97D5B0134DA8E3A4269654BAEC`; `validate-stage.ps1` passou. |
| Regenerar instalador com `serviceworker.js` | Comprovado | O NSIS recompilou o artefato após a correção que faz `/D=` explícito prevalecer sobre o caminho salvo no Registro; o backend foi republicado com as migrações corrigidas, o frontend foi reconstruído, o tratamento de loading foi ampliado na guia de TV, provedores de TV, gravação, editor de imagens, upload/download de imagens, drag-and-drop de playlists, configurações de playback, seleção múltipla, coleções de filmes, detalhes de itens, SyncPlay, listagens, menu de seleção de player, prefetch HLS do HTML5, renderização robusta de PDF/quadrinhos/livros, recomendações, editor de metadados, editores de timers, editores de letras, legendas, uploaders e identificação de itens, o tratamento de falhas na preparação da reprodução foi reforçado, hides legados não podem mais interromper operações concorrentes, o directory browser usa `withLoading`, os editores de acesso/controle parental/perfil exibem erro anunciável e retry, exclusões de dispositivos exibem toast de falha, controladores de Live TV exibem alerta em falhas de carregamento, os players YouTube/PDF/quadrinhos/livros encerram loading em rejeições, o fluxo de seleção do SyncPlay centraliza operações concorrentes, a tela Branding reverte o switch quando a atualização falha, a criação de usuário oferece retry para bibliotecas e canais, a tela de licenças oferece retry da lista de usuários e de licenças individuais, a tela de tarefas oferece retry em falha de carregamento, a tela de bibliotecas oferece retry conjunto de bibliotecas e tarefas, o restore de backup verifica o servidor imediatamente, possui timeout e encerra com erro visível, a tela Networking oferece retry da configuração, o Centro de atualizações, o log de ações, a fila de trabalhos, streaming/resume/trickplay, atividade, relatórios de reprodução, chaves de API, repositórios de plugins, configurações gerais, Live TV e Transcoding oferecem retry, o player YouTube recebeu tipagem corrigida, a ACL usa SIDs universais para Network Service/LocalSystem, o tray foi incluído e `nssm.exe` passou a ser baixado/validado no stage. O instalador atual tem 396.132.961 bytes e SHA-256 `2AACC1E3616ECA7AE8C5511A02227FF22CB6A80C448C0ECA06978EA50A688CFF`. O teste real em VM/host limpo continua pendente. |
| Compatibilidade binária do Nebula no instalador | Corrigido e validado | A instalação reportou `MissingMethodException` para `NebulaFtpConfiguration.get_MaxActiveConnections()`, causada por publicar `MulletaFlix.Server.Implementations.dll` novo com `MediaBrowser.Model.dll` antigo. O servidor foi republicado do zero; reflexão sobre o `MediaBrowser.Model.dll` do stage confirmou `MaxActiveConnections` presente e `validate-stage.ps1` passou. |
| Artefato final desta rodada | Comprovado | O executável NSIS foi recompilado após as correções de Live TV, Transcoding, Display, Metadata e NFO. Tamanho: 396.133.391 bytes. SHA-256: `AEE81DCC9859187B0CF5480E6064F4E20045E6F52EFEF7EB98E8176B66473A0E`. |
| Instalador com ACL reforçada | Comprovado | O NSIS agora cria `data`, `config` e `cache`, habilita herança e concede Modify aos SIDs `S-1-5-20` (NetworkService), `S-1-5-18` (LocalSystem) e `S-1-5-19` (LocalService) antes do primeiro start. Artefato: 396.133.186 bytes, SHA-256 `A34A610A8C744B4C98F55E65E2CBF73A4763C1F3FD02B29F80A17520FC0DE5D8`. |
| Instalador final da rodada | Comprovado | O NSIS foi recompilado após o retry em Branding e mídias não identificadas. Tamanho: 396.133.301 bytes. SHA-256: `FA5B0548BA967EDE387485EB07803E1F75144FE0DC9121B92E1C5B4185B3B635`. |
| Instalador mais recente da rodada | Comprovado | O NSIS foi recompilado após o retry nos alertas de status, saúde, bots e logs do painel Nebula. Tamanho: 396.133.286 bytes. SHA-256: `BFDAFF5C1ECB93935A3494F882F271270218E76A274893509437571101D05953`. |
| Instalador mais recente da rodada | Comprovado | O NSIS foi recompilado após o retry da tela de Dispositivos, que agora refaz a carga de dispositivos e usuários. Tamanho: 396.133.480 bytes. SHA-256: `165A3596A8938D9276BF3DB7E0B584278C8575ECB30D97F9BA50872B64455F28`. |
| Instalador mais recente da rodada | Comprovado | O NSIS foi recompilado após o `ErrorBoundary` passar a normalizar erros lançados como string/objeto e anunciar a falha com `role=alert`. Tamanho: 396.134.068 bytes. SHA-256: `9D9A8E2CDFEA64BDD2716D6396F0FFBE23B1C7BF9B6499863271E1B1D0D2B7C3`. |
| Instalador mais recente da rodada | Comprovado | O NSIS foi recompilado após retry nas telas de Plugins e Usuários, além da normalização robusta do `ErrorBoundary`. Tamanho: 396.133.868 bytes. SHA-256: `125EB8AE95427FB29DCBF598F9CC67DB96EB1AFE0DA06B1330E2B13C3C6B98B8`. |
| Retry coordenado no detalhe de Plugin | Comprovado | As queries de configuração, catálogo de pacotes e plugins instalados agora expõem refetch conjunto; o detalhe exibe retry quando qualquer fonte falha. Typecheck, ESLint dos arquivos alterados e 192 testes frontend passaram. |
| Instalador mais recente da rodada | Comprovado | O NSIS foi recompilado incluindo o retry coordenado do detalhe de Plugin. Tamanho: 396.133.951 bytes. SHA-256: `7F03E3825AA16BDD2E6664443AD8EA920A8383331A0649BE7505B892A4F3DE28`. |
| Loading de pareamento de players | Comprovado no código/testes | O menu de seleção agora encerra o spinner em sucesso, erro ou timeout de 30 segundos; ESLint direcionado, typecheck e 192 testes frontend passaram. |
| Instalador mais recente da rodada | Comprovado | O NSIS foi recompilado incluindo o timeout de pareamento de players. Tamanho: 396.133.574 bytes. SHA-256: `948A876C4D411024EF045E05FBF3A0B15BB7AFB2710AEA99993FDB3A8A6504E8`. |
| Instrumentação de performance | Implementado; execução externa pendente | `scripts/Measure-MulletaFlixEndpoints.ps1` mede `/health`, `/Items/Latest` e detalhe por amostras, calcula min/média/máx e falhas, e salva JSON sem serializar o token. A execução com catálogo real ainda depende de servidor instalado/populado. |
| Recuperação do ErrorBoundary | Comprovado no código/testes | A tela de erro normaliza exceções desconhecidas, anuncia a falha e oferece Retry via reload; ESLint direcionado, typecheck e 192 testes frontend passaram. |
| Instalador mais recente da rodada | Comprovado | O NSIS foi recompilado incluindo o Retry do `ErrorBoundary`. Tamanho: 396.134.268 bytes. SHA-256: `502E07FF30C5C33F9735D3AAE36BD6D1381F736F446B50F33C626933D7334D82`. |
| Detecção/upgrade do serviço NSSM | Corrigido e recompilado | O NSIS não chama mais o comando inexistente `nssm statuscode`, que abria a janela Usage e falsificava a detecção do serviço. Instalação, upgrade e desinstalação usam `sc.exe query MulletaFlixServer`; código 1060 segue para o fluxo sem serviço. Artefato: 396.133.764 bytes, SHA-256 `D3FB788BE92511DAAC4B6745879331E2D9313A1931680CFBFE2629E4637DB1FA`. |
| Upgrade sem desinstalador legado | Corrigido e recompilado | O upgrade não executa mais o `Uninstall.exe` da instalação anterior, evitando comandos antigos do NSSM. O serviço é parado/removido com `sc.exe`, regras de firewall/HTTP são limpas e os dados permanecem. Instalador final com backend republicado: 396.134.144 bytes, SHA-256 `8760E1A17EF8B056744C930D322C31A8D56696DAAC1448B652C9B34864CA17CE`. |
| Recuperação de conflito na porta 8096 | Corrigido no backend | Após liberar o PID conflitante, o retry agora reseta o host Kestrel parcialmente iniciado antes de chamar `StartAsync` novamente, evitando o erro secundário `Server has already started`. |
| MariaDB no modo básico | Corrigido e recompilado | O log real mostrou `InnoDB operating system error number 5/203` ao abrir `mariadb_data`, porque uma instalação sem serviço usava `ProgramData` sem ACL de escrita para o usuário interativo. O NSIS agora direciona Basic para `LocalAppData` e aplica ACL Modify ao usuário atual em caminhos customizados. O backend não mascara mais falhas de inicialização/porta do MariaDB. Artefato: 396.134.661 bytes, SHA-256 `713512CBA90F226A8BBA0EF49C523C1AA1D104E9056059F49A81D35F8E45A932`. |
| Wizard em tela preta | Corrigido e recompilado | A rota carregava, mas o `ViewManager` não encontrava `.mainAnimatedPages`; o layout `ThemedRootAppLayout` renderizava o `Outlet` sem `AppBody`. A correção restaura o contêiner legado e foi validada por TypeScript, ESLint e 192 testes do frontend. O instalador final atualizado tem 396.135.203 bytes e SHA-256 `D4A1DB29919F597100F5F3F0C7CB0621CC5F562030E77EF8912C3B1B256764C3`. |
| Dashboard sem feedback ao falhar tarefas | Corrigido | A página inicial agora exibe `Alert` acessível e botão de retry quando a consulta de tarefas falha, em vez de descartar o erro silenciosamente. |
| Falha silenciosa em telas legadas | Corrigido | `ViewManagerPage` agora registra a exceção, mostra `role=alert` e oferece Retry quando o controller/view não pode ser importado; antes o erro era descartado e a rota podia ficar vazia. |
| Midia Storage Online sem estado de tarefas | Corrigido | A tela agora diferencia falha da consulta de tarefas de uma lista vazia e oferece retry sem bloquear a edição da configuração. |
| Instalador após a segunda rodada de UX | Recompilado e validado | O artefato inclui o feedback recuperável do `ViewManager` e do Midia Storage Online. Tamanho: 396.135.533 bytes; SHA-256: `3A61D43C31714A88A41B33678C47B0A647C617B60ED20EEC7C7E99137F51E32C`. `validate-stage.ps1` passou. |
| Cache de assets após upgrade | Corrigido | O service worker passou a usar `mulletaflix-shell-v12` e remove caches `mulletaflix-shell-*` antigos durante `activate`, reduzindo regressões causadas por assets obsoletos. TypeScript, ESLint e validação de artefatos passaram. |
| Instalador com cache versionado | Recompilado e validado | Artefato final inclui a atualização do service worker, além das correções anteriores. Tamanho: 396.135.652 bytes; SHA-256: `A35DF267420C941F74BC00CD07DD667875F485E85C7BCFC7EFABCCEA540281E9`. |
| Erro “Could not stop service” no desinstalador | Corrigido e compilado | O fluxo de desinstalação agora usa o SCM (`sc.exe`) e trata códigos 1060/1062 como estados idempotentes, sem chamar NSSM para parar/remover o serviço. O NSIS compilou com sucesso; stage validado. Artefato: 396.135.654 bytes, SHA-256 `4B56332F56E9F6FC3C9B6867B65D5B08A36A5CA611CDE2C99C2FA4EA476E26E7`. |
| Dados bloqueados durante desinstalação | Corrigido | A opção de excluir dados agora é registrada e executada somente após o serviço e processos auxiliares serem encerrados, reduzindo sobras de `config`, banco e cache. Artefato: 396.135.716 bytes, SHA-256 `EA3E8736D37D52EE0BE60EA4B26C9C49617A8D217A181510F197E7199EEC70FD`. |
| Diagnóstico do smoke test | Melhorado | `Test-CleanInstaller.ps1` agora registra estado/conta/exit code/path do serviço e as últimas linhas dos logs antes de relançar a falha, acelerando a investigação de ACL, MariaDB, serviço ou endpoint. Parser PowerShell passou. |
| Segredo em fixture de teste | Corrigido | A chave TMDB literal do fixture `American Gods.nfo` foi substituída por `TEST_KEY`; a suíte XbmcMetadata passou com 38/38 testes. |
| Dependências frontend | Sem vulnerabilidades reportadas | `npm audit --omit=dev --audit-level=moderate` e `npm audit --audit-level=high` retornaram 0 vulnerabilidades; inventário: 226 dependências de produção e 1.128 totais resolvidas. |
| Dependências .NET | Sem vulnerabilidades reportadas | `dotnet list MulletaFlix.sln package --vulnerable --include-transitive` consultou `https://api.nuget.org/v3/index.json` e retornou nenhum pacote vulnerável em todos os projetos avaliados. |
| Playback Reports sem erro de estatísticas | Corrigido | A falha de `usePlaybackReportStats` agora participa do estado de erro da tabela e pode ser recuperada pelo retry conjunto de usuários, relatórios e estatísticas. ESLint, TypeScript e 192 testes frontend passaram. |
| Instalador após correção de Playback Reports | Recompilado e validado | Artefato com o frontend atualizado: 396.135.203 bytes; SHA-256 `D4A1DB29919F597100F5F3F0C7CB0621CC5F562030E77EF8912C3B1B256764C3`; `validate-stage.ps1` passou. |
| Exportação CSV dos relatórios | Corrigido e validado | A falha da exportação deixou de ser descartada silenciosamente: a tela agora registra o erro e exibe feedback ao usuário quando não há servidor ou o download falha. TypeScript, ESLint direcionado e os testes do frontend foram executados após a alteração. |
| Instalador após feedback da exportação CSV | Recompilado e validado | Frontend republicado no stage e NSIS recompilado. Tamanho: 396.135.366 bytes; SHA-256 `4FECBCD4A92877045AA56E63CAE4EA172AC61BCB161B54F965A34BF6F3597D13`; `validate-stage.ps1` passou. |
| Permissões do serviço na primeira inicialização | Corrigido no instalador | A instalação seguinte avançou além do Nebula, mas a migração falhou ao salvar `config/system.xml` com `UnauthorizedAccessException`, pois `NetworkService` não tinha escrita no diretório de dados. O NSIS agora executa `icacls` recursivo antes de iniciar o serviço e aborta com mensagem clara se a ACL não puder ser aplicada. |
| Migrações legadas em instalação nova | Corrigido no backend | `MigrateNetworkConfiguration` e `MigrateEncodingOptions` agora ignoram arquivos ausentes sem registrar erro, tratam XML incompatível como warning recuperável e `MigrateEncodingOptions` usa os campos corretos para `EncoderPreset` e `DeinterlaceMethod`. |
| Splash inicial do instalador | Corrigido e smoke-testado | O bundle antigo travava com `TypeError: Cannot read properties of undefined (reading 'location')` em `AppRouter`, porque imports estáticos podiam construí-lo antes da history do React Router. `AppRouter` agora tolera a criação antecipada e conecta a listener quando `ROUTER_HISTORY_READY` é emitido. O stage atualizado respondeu 200 em Playwright, montou 3 filhos em `#reactRoot`, removeu `.splashLogo` após 5s e não produziu `pageerror`; o único log restante foi um 404 esperado do servidor estático sem backend. |
| Chunks acima de 500 KB | Implementado | O build atual mede o maior chunk JS em aproximadamente 445 KB (`vendor-jellyfin`) e o maior CSS em aproximadamente 258 KB; nenhum JS/CSS de `assets/` ultrapassa 500 KB. As dependências React/Jellyfin/EPUB também foram particionadas sem ciclos. |
| Consolidação global de loading/erro | Parcial implementado | `loading.withLoading` agora equilibra o spinner via `finally`; wizard, catálogo, listagens e recomendações de música/TV, guia de TV, editor de metadados, editores de timers, editores de letras e legendas, uploaders de letras e legendas, favoritos, detalhes de mídia, principais controllers de Live TV, telas de usuário do dashboard, ações de timers, perfil do usuário, configurações da tela inicial, tuner picker, dispositivos e fluxos de criação/upload/download de imagens agora encerram o loading também nas rejeições e oferecem retry onde aplicável. As preferências experimentais de exibição agora aceitam o usuário atual e exibem erro/retry; Networking não esconde mais o erro atrás de loading infinito. Ainda falta completar a matriz por demais telas legadas/dashboard, com erro anunciável, retry e estados vazios distintos. |
| Lista principal de usuários | Corrigido no código/testes | `routes/users/index.tsx` agora diferencia carregamento de falha da API, anuncia o erro com `role=alert` e oferece Retry; typecheck, ESLint direcionado e 192 testes frontend passaram. |
| Smoke Playwright local | Bloqueado por configuração externa | O Vite dev server sobe normalmente após restringir `optimizeDeps.entries` a `index.html`; a suíte wizard não inicia sem `MFLX_ADMIN_USER` e `MFLX_ADMIN_PASSWORD`. O smoke equivalente permanece coberto pelo job `e2e-smoke` do CI. |
| Migração JS para TypeScript | Implementado | `src/index.jsx` foi migrado para `src/index.tsx`; não restam arquivos `.js` ou `.jsx` sob `src`, e o typecheck/build permanecem limpos. |
| Redução de `any` público | Implementado no contrato legado principal | `apiclient.d.ts` agora não contém ocorrências de `any`: parâmetros usam `LegacyRequestOptions`/contratos locais, `getJSON<T>()` é genérico e retornos usam tipos específicos, `unknown` ou `Response`; consumidores de login, recomendações, biblioteca, acesso de usuário e reset de senha foram ajustados e o typecheck permanece limpo. |
| TODOs críticos de playback | Implementado/documentado | O enfileiramento de fotos respeita `options.startIndex`, o dispatcher `stop` não envia mais o segundo argumento obsoleto, a detecção de bitrate foi simplificada com fallback explícito, o skip button é montado no OSD quando disponível, `StartIndex` de episódios/fotos viaja como metadado da tradução sem mutar a entrada, a troca de player preserva o item explícito e a seleção forçada de player local é documentada como requisito para inspeção de fontes. |
| Separação de alterações locais | Pendente de decisão de integração | Classificar arquivos por entrega, criar commits/branches independentes e preservar alterações pré-existentes identificadas pelo proprietário do workspace. |
| Recovery seguro de porta | Corrigido e testado | O recovery não encerra mais qualquer PID encontrado no `netstat`: ele compara o caminho completo do executável do processo conflitante com o executável atual, recusa caminhos desconhecidos ou externos e retorna erro controlado quando a porta permanece ocupada. `PortBindingRecoveryTests` passou 2/2. |
| Instalador após proteção do recovery de porta | Recompilado e validado | Backend republicado, NSIS recompilado e `validate-stage.ps1` aprovado. Artefato: `mulletaflix_12.0.0_windows-x64.exe`, 377.79 MB, SHA-256 `61867BA4A67B23DA2E2B8AF231931231AF34F22D29F88B6307686A1AE97641D5`. |
| Nebula em instalação nova | Corrigido no código | Nebula agora começa desabilitado por padrão; somente uma configuração existente explicitamente habilitada continua ativa. O smoke test usa `/ready`, que valida o servidor/banco sem transformar uma integração opcional não configurada em falha da instalação. A cobertura confirma ausência de tokens/API hash e o novo default desabilitado. |
| Runtime do stage após default seguro do Nebula | Comprovado | O stage iniciou com `--datadir` isolado, concluiu `Startup complete` e respondeu `200 Healthy` em `http://127.0.0.1:18096/ready`; a instância real em 8096 não foi interrompida. O diretório temporário foi removido após o teste. |
| Primeira execução sem configuração Nebula | Comprovado | Um stage foi iniciado sem `config/nebulaftp.xml` pré-existente, concluiu `Startup complete`, registrou `NebulaFTP está desabilitado na configuração` e respondeu `200 Healthy` em `http://127.0.0.1:18097/ready`. O processo foi encerrado e o diretório temporário removido; a instância real em 8096 permaneceu no PID esperado. |
| Isolamento do diretório de dados no smoke test | Corrigido | O instalador agora aceita `/DATA=<caminho>` e o `Test-CleanInstaller.ps1` passa esse argumento ao NSIS; o teste deixou de aceitar `-DataDirectory` sem que o instalador o respeitasse. Parser PowerShell passou. |
| Parada segura de processos no upgrade/desinstalação | Corrigido | Removido o `TaskKill /IM` global, que podia encerrar processos de outras instalações. O instalador agora filtra por caminho do executável/linha de comando e usa namespaces corretos no desinstalador NSIS. Scripts PowerShell passaram no parser; a compilação NSIS foi validada após corrigir a função `un.`. |
| Espera do estado real do serviço | Corrigido | Após `sc stop`, o instalador agora aguarda o SCM confirmar `Stopped` por até 30 segundos; timeout aborta o upgrade ou oferece retry na desinstalação, evitando tentar remover um serviço ainda em `STOP_PENDING`. |
| Scripts de parada/espera do instalador | Validado localmente | O script de espera retornou `1` ao detectar o serviço real em execução sem modificá-lo; o filtro de processos retornou `0` sem alvos fora dos diretórios informados. Parser PowerShell e compilação NSIS passaram. |
| Fronteiras das alterações locais | Documentado | `docs/CHANGESET-BOUNDARIES.md` separa backend, frontend, packaging, documentação, skills e arquivos fora das fronteiras; nenhum commit foi criado automaticamente. |
| Ambiente para smoke test elevado | Bloqueado por capacidade do host | O host atual é Windows 10 Pro, mas Windows Sandbox, Hyper-V, VirtualBox, VMware e Docker não estão disponíveis; somente WSL está instalado, sem fornecer um SCM Windows isolado. A instalação real continua preservada e não foi interrompida para simular uma VM. |
| Empacotador legado | Isolado/documentado | A cópia `mulletaflix-server-windows-mulletaflix-installer-branding` contém scripts históricos e não participa do build atual; o README agora direciona para `MulletaFlix-packaging-master`, evitando gerar releases com o fluxo antigo. |
| Instalador final com frontend atualizado | Recompilado e validado | Frontend republicado no stage após as últimas correções, tray compilado sem warnings/erros, NSIS recompilado e `validate-stage.ps1` aprovado. A instância real permaneceu ativa. Artefato: `mulletaflix_12.0.0_windows-x64.exe`, 396.140.350 bytes, SHA-256 `AE799C4D4C7A53A40A1FE82C509A6F45A1EBFB5B2DF5AD1BDC17FDC4392CB6DC`. |
| Correção do wizard travado em tela preta | Reproduzido, corrigido e recompilado | O teste Playwright reproduziu a ausência de `#wizardStartPage` no servidor instalado. `ConnectionRequired` agora monta o `Outlet` assim que o cliente público está disponível; TypeScript, integridade do stage e NSIS passaram. Artefato: `mulletaflix_12.0.0_windows-x64.exe`, 396.140.968 bytes, SHA-256 `55B750245E00561736A5618FD451C863DAF8CA8029D22F1313932ED62C60411F`. |
| Correção final do loading do wizard | Recompilado e validado | O cliente agora é registrado antes da segunda consulta de informações públicas, evitando bloqueio quando o servidor ainda está concluindo o boot. TypeScript e `validate-stage.ps1` passaram. Artefato: `mulletaflix_12.0.0_windows-x64.exe`, 396.140.397 bytes, SHA-256 `001DAA78DA8FF9FE0E2A95DCDD90C710B8D88AA2FD82D7257C0B4E0B88037F00`. |
| Script NSIS efetivamente compilado | Recompilado e validado | O build usa `MulletaFlix-ux-custom/nsis/mulletaflix.nsi`, cujo fluxo de upgrade já consulta/paralisa/remove pelo SCM (`sc.exe`) e não executa o `Uninstall.exe` legado. Artefato: `mulletaflix_12.0.0_windows-x64.exe`, 396.140.397 bytes, SHA-256 `15AC56952808D3A3261307133B616460DC5E13446FA0BDDA7918873E6869A4EE`; `validate-stage.ps1` passou. |
| ACL HTTP do Nebula | Corrigido e recompilado | A reserva HTTP.sys de `http://+:2123/` deixou de conceder acesso a todos (`WD`) e agora permite somente NetworkService, LocalSystem e LocalService. O smoke test valida a presença desses SIDs e rejeita ACL ampla. Artefato: `mulletaflix_12.0.0_windows-x64.exe`, 396.140.406 bytes, SHA-256 `1E4CA089A7C0451597B24A1F16BE9F7F94A8883E2A5E121E3B746790D427FA96`; `validate-stage.ps1` e o parser PowerShell passaram. |
| Bundle final do wizard | Recompilado e validado | Após a limpeza do lint focado em `ConnectionRequired`, o stage passou a usar `assets/index-BCekr36A.js`. Artefato: `mulletaflix_12.0.0_windows-x64.exe`, 396.140.384 bytes, SHA-256 `839F055F4780DC048A6F100770891D98C1DF5974B60DCBDBAACE02BFC3E1F900`; lint focado, TypeScript e `validate-stage.ps1` passaram. |
| Integridade dos pré-requisitos | Corrigido e recompilado | Antes de executar os downloads de Python e WinFsp, os scripts agora exigem assinatura Authenticode válida; para Python, o certificado também deve pertencer à Python Software Foundation. Os guards foram confirmados no stage. Artefato: `mulletaflix_12.0.0_windows-x64.exe`, 396.140.681 bytes, SHA-256 `A51FFA23FCE4CF69DBDE568B00447AE5C4B379B6C4970F394B336C3F1CB43A4E`; `validate-stage.ps1` passou. |
| MariaDB embutido exposto à rede | Corrigido e recompilado | O banco interno, que usa credenciais locais simples, agora inicia com `--bind-address=127.0.0.1`; o NSIS também remove a regra ampla `MulletaFlix MariaDB` de instalações anteriores e o smoke test rejeita sua presença. Artefato: `mulletaflix_12.0.0_windows-x64.exe`, 396.140.690 bytes, SHA-256 `860167A7AD6F85ED85C1741B1D656A69DA5B7A82F2AF1991D950F5F15CFA5E7C`; `dotnet build` e `validate-stage.ps1` passaram. |
| Upgrade básico preservava servidor antigo | Corrigido e recompilado | A pré-checagem abortava o upgrade quando encontrava `MulletaFlix.exe`, antes de alcançar o encerramento seguro por diretórios de instalação/dados. Ela foi removida: o upgrade agora encerra somente os processos pertencentes à instalação em atualização e substitui o bundle web antigo. Artefato: `mulletaflix_12.0.0_windows-x64.exe`, 396.137.487 bytes, SHA-256 `17026CD8F38311FDD033F5C7B6B5B89E2114A74E34DBA3317F2809E7170B9505`; `validate-stage.ps1` passou. |
| Nebula bloqueava caminhos personalizados | Corrigido e recompilado | O helper de encerramento deixou de assumir `Program Files`; ele recebe o diretório de instalação e finaliza somente processos cujo executável pertence ao respectivo `nebula`. A rotina também passou a ser chamada na desinstalação. Artefato: `mulletaflix_12.0.0_windows-x64.exe`, 396.137.856 bytes, SHA-256 `225B4162764F6D8E549DCF0F9A5121F378AA68E723D026452620D0AD9A94882B`; `validate-stage.ps1` passou. |
| Download mutável do rclone | Corrigido e recompilado | O bootstrap não aceita mais `rclone-current-windows-amd64.zip`. Ele baixa o release oficial fixado `v1.75.1` e exige SHA-256 `200EB602C126D82AA38B51E0F6B9AE837473FF99B51278D3F6F837574C494D6E` antes de extrair ou executar o binário. Artefato: `mulletaflix_12.0.0_windows-x64.exe`, 396.138.041 bytes, SHA-256 `E73255D5F5496A6D9C8A97FC2554F707F7EC933398A95D79CA0BD4201BDAC701`; `validate-stage.ps1` passou. |
| MongoDB exposto e MSI sem verificação | Corrigido e recompilado | O instalador passou a validar a assinatura Authenticode do MSI do MongoDB antes de executá-lo. As duas criações da regra `MulletaFlix MongoDB` foram removidas, pois o Nebula usa `mongodb://localhost:27017`; o smoke test rejeita uma regra de entrada residual. Artefato: `mulletaflix_12.0.0_windows-x64.exe`, 396.138.017 bytes, SHA-256 `0D84128610BDEE09E8F7A0F8B15985E9BE604C2C7635C8B88F09394473BC5303`; `validate-stage.ps1` e parser do smoke test passaram. |
| Warnings acionáveis do frontend | Reduzidos e validados | `FileReader` deixou de usar constantes depreciadas de `DOMException`, os `FIXME` de opções legadas foram convertidos em notas de compatibilidade, o harness Cucumber passou a ter sua convenção CommonJS/DSL documentada no escopo correto e as expectativas numéricas de layout usam tolerância de precisão alta. O gate incremental atual verifica 253 arquivos, com 0 erros e 51 warnings não bloqueantes; build TypeScript e 196 testes passaram. O lint global permanece informativo por conter dívida legada, principalmente `no-explicit-any`. |
| Dependências backend | Verificado | `dotnet list Jellyfin.Server/Jellyfin.Server.csproj package --vulnerable --include-transitive` não encontrou pacotes vulneráveis nas fontes NuGet atuais. |
| Tipagem de elementos de UI | Corrigido e validado | Estilos de legenda usam `setProperty`, o protótipo de radio tem ciclo de vida tipado e o slider de tabs usa `HTMLElement` diretamente; três casts `any` foram removidos. ESLint e typecheck passaram. Os cinco aliases `this` restantes em `emby-tabs` são callbacks do Web Component legado. |
| Tipagem do scroller React | Corrigido e validado | `emby-scroller/Scroller.tsx` usa os parâmetros do construtor legado diretamente, removendo um cast `any` sem alterar as opções de rolagem. ESLint e typecheck passaram. |
| Protótipos de elementos customizados | Corrigido e validado | `emby-checkbox.ts` e `emby-progressbar.ts` passaram a declarar os callbacks de ciclo de vida no próprio tipo do protótipo, removendo quatro casts `any`. ESLint e typecheck passaram; os cinco avisos `webkitAnimation*` permanecem por compatibilidade com TVs antigas. |
| Tipagem das integrações de reprodução | Corrigido e validado | `themeMediaPlayer.ts` e `playAccessValidation/plugin.ts` removeram dois casts `any`, validam usuário ausente antes de consultar mídia temática e preservam a restrição de reprodução quando a política não está explicitamente habilitada. ESLint e typecheck passaram. |
| Cancelamento do Book Player | Corrigido e validado | O carregamento EPUB agora interrompe imediatamente após cancelamento e rejeita com erro contextual; falhas de renderização também propagam erro explícito. ESLint, typecheck e 192 testes frontend passaram. |
| Atualização do botão de gravação | Corrigido e validado | O construtor do `RecordingButton` agora passa `serverId` e `itemId` na ordem correta ao atualizar um item; antes os argumentos eram invertidos. O cliente/API foi tipado, falhas de refresh são tratadas e ESLint/typecheck passaram. Não existe teste unitário específico desse componente no repositório. |
| Tipagem de restauração do AutoCast | Corrigido e validado | `autocast.ts` deixou de tipar alvos de reprodução como `any[]`; o tipo agora é derivado de `playbackManager.getTargets()`, incluindo o callback de seleção. ESLint e typecheck passaram. |
| Espelhamento de conteúdo | Corrigido e validado | `displayMirrorManager.ts` removeu um cast `any` desnecessário na ponte para o SDK, mantendo o comportamento de espelhamento e a compatibilidade com o cliente legado. ESLint e typecheck passaram. |
| Tipagem da fila de playback | Corrigido e validado | `controllers/playback/queue/index.ts` passou a inferir o tipo do player a partir do `playbackManager`, removendo dois `any` explícitos e uma variável morta no release. ESLint e typecheck passaram. |
| Gerenciador de screensaver | Corrigido e validado | `screensavermanager.ts` removeu seis usos de `any`, tipou o singleton e o plugin, e ignora payloads inválidos de `playbackstop` em vez de lançar exceção durante eventos externos. ESLint, typecheck e busca de `any` no arquivo passaram. |
| Tipagem e montagem de playlists | Corrigido e validado | `playlistViewer.ts` removeu 14 usos de `any`, tipou o cliente API, resposta de itens e container de drag-and-drop, e evita dereference quando o container ainda não foi montado. ESLint, typecheck e busca de `any` no arquivo passaram. |
| Notificações WebSocket do servidor | Corrigido e validado | `serverNotifications.ts` removeu 21 usos de `any`, tipou comandos de reprodução/estado e o cliente API, e passou a tratar callbacks externos como payloads desconhecidos antes do uso. ESLint, typecheck e busca de `any` no arquivo passaram. |
| Tipagem do carregamento por nome | Corrigido e validado | `itemsByName.ts` substituiu oito usos de `any` por contratos de consulta e `BaseItemDtoQueryResult`, trata `TotalRecordCount` ausente com segurança e mantém os objetos compatíveis com os builders de lista/cartões. ESLint, typecheck e busca de `any` no fluxo passaram. |
| Carregamento de conteúdo de servidor | Corrigido e validado | `ServerContentPage` deixou de depender de casts `any`, valida a existência do cliente API antes da requisição e usa o contrato tipado de `getUrl`/`get`; a compatibilidade do cast legado do SyncPlay foi explicitada. Typecheck e ESLint passaram, com apenas um aviso de aninhamento de funções. |
| Listener do AlphaPicker | Corrigido e validado | O callback usado por `removeEventListener` agora é a mesma referência registrada por `addEventListener`; antes, um novo `bind` impedia a remoção e acumulava listeners a cada reativação. ESLint direcionado e typecheck passaram. A busca por `removeEventListener(...bind(...))` não encontrou outros casos. |
| Colisão de porta durante startup | Diagnóstico confirmado | O log de 2026-09-12 registra uma tentativa secundária de bind em `0.0.0.0:8096` enquanto a instância principal já ocupava a porta. A tentativa falhou com `AddressInUseException`; o processo principal permaneceu saudável, `/health` e `/ready` responderam 200, e o upload Nebula seguinte foi concluído. |
| Loading infinito do wizard | Corrigido e validado | A causa era o uso de rotas `lazy` de data-router dentro de `useRoutes`, deixando o `Outlet` sem página renderizável. As rotas legadas agora usam `element: <ViewManagerPage />`; os testes Playwright de abertura e conclusão do wizard passaram (2/2). Instalador: 399.531.261 bytes; SHA-256 `AE2A4D1C5E218C42A45329E5D91AC67B14A6DE4D7AE3239D06AA219E5EAF905B`. |
| Plugins externos incompatíveis no primeiro arranque | Corrigido | O bootstrap automático foi desativado para os pacotes `Merge Versions` e `Theme Songs`, que geravam `MissingMethodException` contra a API atual. A instalação manual continua possível após validação de ABI; teste de catálogo garante que o bootstrap não instala plugins de terceiros automaticamente. |
| Smoke do instalador após proteção de plugins | Comprovado | O stage limpo passou os 2 testes do wizard e seu log não registrou `Merge Versions`, `Theme Songs`, `MissingMethodException` ou falha de atualização de plugins. Artefato final: 399.531.141 bytes; SHA-256 `65DA3C8BA86E273B24E543CB20C19CC830C7F10F820867E6A18ACABE59ADA0BB`. |
| Smoke test sem isolamento suficiente | Corrigido no harness | `Test-CleanInstaller.ps1` agora recusa chaves de registro MulletaFlix existentes e portas 8096/3306 ocupadas antes de executar o instalador. O instalador usado pelo builder reconhece `/TESTMODE` e `/DATA=`, evitando upgrade acidental da instalação real. O teste final em VM limpa ainda é necessário para validar o fluxo completo. |
| MariaDB em runtime instalado | Confirmado após correção | A instância local registrou `MariaDB is accepting SQL connections after 3826ms` em 2026-09-12 13:12:24 e `http://127.0.0.1:8096/ready` respondeu HTTP 200. |
| Imagens dos providers AudioDB/MusicBrainz | Corrigido e testado | Quatro `GetImageResponse` que lançavam `NotImplementedException` agora usam `IHttpClientFactory`/`NamedClient.Default`; teste focalizado passou 4/4 e a suíte `Jellyfin.Providers.Tests` passou 346/346. O instalador foi republicado com essa correção: 396.139.539 bytes, SHA-256 `532876335B3003CD43AB0CA2F1DC43E47DB4E917013A0CB4A945152697BF5504`. |
| Tela preta após concluir o wizard | Corrigido e recompilado | A delegação dinâmica do roteador raiz usava `path: '*'`, consumindo a URL antes da árvore `useRoutes` de dashboard/home; passou a usar `path: '/*'`. O bundle final referencia `index-CF-E2uQq.js`, o stage foi validado e o instalador final tem 396.140.704 bytes, SHA-256 `9DC1582F496E022201DEE8DC5E103A36A8CD1AD560DBE08D5B1BF4AC6CF32906`. |
| Reinício após mutex MariaDB abandonado | Corrigido e recompilado | `AcquireStartupMutex` agora trata `AbandonedMutexException` como posse transferida pelo sistema operacional, inclusive no fallback sem namespace global. O stage iniciou depois de um mutex abandonado sem repetir a exceção; `dotnet build` passou e o instalador contém a DLL corrigida. |
| Encerramento do watcher Nebula com cancelamento | Corrigido e testado | `NebulaStagingWatcher.StopAsync` agora trata o cancelamento normal dos workers ao aguardar `Task.WhenAll`, evitando que `DisposeAsync` propague `TaskCanceledException`. A suíte de implementação passou 765 testes, com 38 ignorados. |
| Recuperação repetitiva da unidade N: | Corrigido no watchdog | O watchdog não agenda mais tentativas concorrentes a cada 30 segundos. Após falhas, aplica backoff progressivo até 10 minutos e continua tentando; após sucesso, o intervalo é resetado. |
| Artefato final após correção do watcher | Recompilado e validado | Instalador `mulletaflix_12.0.0_windows-x64.exe` gerado com 396.141.177 bytes e SHA-256 `8ECBE5984A3AB0777CB05C139E2580955043554856442C6D2E4E3FB6A7332C3E`; `validate-stage.ps1` passou. |
| HTTP do Nebula em instalação básica | Corrigido no instalador | Instalações sem serviço agora reservam apenas `127.0.0.1:2123` e `localhost:2123` para o usuário interativo atual; instalações como serviço continuam usando a ACL wildcard restrita às três contas de serviço. |
| Artefato final após correção da ACL HTTP básica | Recompilado e validado | Instalador `mulletaflix_12.0.0_windows-x64.exe` gerado com 396.141.288 bytes e SHA-256 `C4A6F3EEE8279F56DED1D39F4EBA49631DF291BFEBFF77AEB96FE02BA059E2C8`; `validate-stage.ps1` passou. |
| Artefato final após limpeza das ACLs loopback | Recompilado e validado | Instalador `mulletaflix_12.0.0_windows-x64.exe` gerado com 396.141.327 bytes e SHA-256 `6890E7C0198FAC9BC9E6969E04C8E86FC5C38A3404C917A10D02A2C1B99A8640`; `validate-stage.ps1` e parser do smoke test passaram. |
| Limpeza do smoke test após falha do instalador | Corrigido no harness | A execução do instalador passou a ocorrer dentro do `try/finally`; se a instalação falhar depois de criar `Uninstall.exe`, o smoke test ainda executa a desinstalação recuperável. Parser PowerShell aprovado. |

## Gates já comprovados

O build de produção atual foi revalidado com maior chunk JS de aproximadamente 445 KB (`vendor-jellyfin`) e maior CSS de aproximadamente 258 KB. A separação segura de React, Jellyfin, EPUB, Router, DOMPurify, Axios, React Query e date-fns, o lazy loading de React Query Devtools, a árvore dinâmica de rotas e o particionamento de fontes CJK mantêm todos os JS/CSS abaixo de 500 KB sem ciclos.

- Frontend: `npm run build:check`, `npm run lint:changed` e `npm run test -- --run`.
- Frontend: suíte atual com 21 arquivos e 196 testes aprovados.
- Packaging: `scripts/validate-stage.ps1` aprovado no stage conhecido.
- Backend: publicação Release concluída sem erros; `Jellyfin.Server.Tests` aprovou 16 testes, a suíte de integração aprovou 111/114 (3 ignorados) e a suíte Nebula focada permanece registrada com 149 testes aprovados.
- Integridade: `git diff --check` aprovado.
